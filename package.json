{
  "name": "bookstore",
  "version": "1.0.0",
  "description": "book store app",
  "main": "app.js",
  "scripts": {
    "start": "node app",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "body-parser": "*",
    "cors": "*",
    "express": "*",
    "mongoose": "*",
    "node-schedule":"*"
  },
  "author": "Makakura",
  "license": "ISC",
  "devDependencies": {
    "multer": "^1.3.0"
  }
}
